<!DOCTYPE html>
<html lang="hr">
  <head>
    <%- include('../partials/_head') %>
    <link rel="stylesheet" href="/styles/login.css" />
    <script src="/scripts/login.js" defer></script>
  </head>
  <body>
    <main class="card">

      <form id="loginForm" autocomplete="on" novalidate>
        <div class="field">
          <label for="email" class="label">Elektronička pošta</label>
          <input
            id="email"
            name="email"
            type="email"
            required
            placeholder="elektronicka.posta@hotmail.com"
            class="input"
          />
        </div>

        <div class="field">
          <label for="password" class="label">Lozinka</label>
          <input
            id="password"
            name="password"
            type="password"
            required
            minlength="6"
            placeholder="********"
            class="input"
          />
          <button type="button" class="toggle-btn" id="togglePassword">
            Skrivena šifra 🙈
          </button>
        </div>

        <div id="feedback" class="feedback" role="status" aria-live="polite"></div>

        <button id="submitBtn" type="submit" class="btn">Ulazak</button>
      </form>
    </main>
  </body>
</html>
